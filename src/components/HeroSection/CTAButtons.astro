---
import { buttonVariants } from '@/components/ui/button';
import { cn } from '@/lib/utils';
---

<div class="w-full flex flex-col items-center md:items-start gap-4 md:gap-6">
  <div class="w-full flex gap-4 flex-wrap justify-center md:justify-start">
    <a
      href="#projects"
      class={cn(
        buttonVariants({ variant: 'default' }),
        'flex-1 max-w-[10rem] md:text-base md:h-10 md:px-6 md:has-[>svg]:px-4'
      )}>View My Work</a
    >
    <a
      href="#contact"
      class={cn(
        buttonVariants({ variant: 'outline' }),
        'flex-1 max-w-[10rem] md:text-base md:h-10 md:px-6 md:has-[>svg]:px-4'
      )}>Contact Me</a
    >
  </div>
  <!-- <Button variant="ghost" className="md:text-base w-fit md:h-10 md:px-6 md:has-[>svg]:px-4"
    >Download Resume <ArrowDownToLine className="size-3.5 -mb-0.5 md:-mb-1" /></Button
  > -->
</div>
